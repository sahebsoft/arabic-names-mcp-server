apiVersion: v1
kind: Service
metadata:
  name: arabic-names-mcp-server-service
  namespace: arabic-names
  labels:
    app: arabic-names-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: arabic-names-mcp-server